{
  "name": "@dbut2/brand",
  "version": "0.1.0",
  "private": true,
  "description": "dbut2 brand",
  "scripts": {
    "build": "tailwindcss -i src/app.css -o dist/dbx.css --minify",
    "watch": "tailwindcss -i src/app.css -o dist/dbx.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.12",
    "tailwindcss": "^4.1.12"
  }
}
